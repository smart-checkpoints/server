node_modules

# The SQLite database and its write-ahead log.
database.db
database.db-shm
database.db-wal

.env

# The console's build output. `npm run build` regenerates it; the server serves
# it from where Next.js writes it.
/console/out/
/console/.next/
/console/next-env.d.ts
