{
  "name": "smart-checkpoints-server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "build": "node scripts/build-console.js",
    "start": "node server.js",
    "dev": "node server.js",
    "console:dev": "npm --prefix console run dev",
    "console:lint": "npm --prefix console run lint"
  },
  "keywords": [
    "average speed enforcement",
    "section control",
    "traffic enforcement",
    "checkpoint graph",
    "smart checkpoints"
  ],
  "author": "",
  "license": "MIT",
  "description": "The Smart Checkpoints server: the checkpoint graph, the enforcement path, and the operator console that drives them.",
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "socket.io": "^4.8.3",
    "sqlite3": "^5.1.7",
    "ws": "^8.19.0"
  },
  "private": true,
  "engines": {
    "node": ">=20"
  }
}
